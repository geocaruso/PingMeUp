# Summary (wrapper) of player counts and tables

Summary (wrapper) of player counts and tables

## Usage

``` r
players.summary(players = NULL)
```

## Arguments

- players:

  A players data.frame. Defaults to `players_m`, but you may supply
  `players_f` or any archived snapshot.

## Value

A list containing counts and tables for the supplied dataset.
