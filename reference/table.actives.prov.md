# Active players per province

Active players per province

## Usage

``` r
table.actives.prov(players = NULL)
```

## Arguments

- players:

  A players data.frame. Defaults to `players_m`, but you may supply
  `players_f` or any archived snapshot.

## Value

A table of active players by province.
