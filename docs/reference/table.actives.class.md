# Active players per classement

Active players per classement

## Usage

``` r
table.actives.class(players = players_m)
```

## Arguments

- players:

  A players data.frame. Defaults to `players_m`, but you may supply
  `players_f` or any archived snapshot.

## Value

A table of active players by classement.
