# Active players per classement letter

Active players per classement letter

## Usage

``` r
table.actives.class.l(players = NULL)
```

## Arguments

- players:

  A players data.frame. Defaults to `players_m`, but you may supply
  `players_f` or any archived snapshot.

## Value

A table of active players by classement letter.
