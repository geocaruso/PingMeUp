% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.players_registry.R
\docType{data}
\name{players_m}
\alias{players_m}
\alias{players_m_archive}
\alias{players_f}
\alias{players_f_archive}
\title{Players Registry (Latest Snapshot) and Archive}
\description{
These datasets store the latest and archived snapshots of the AFTT
players registry (Messieurs and Dames) as produced by
\code{build.players.registry.m()} and \code{build.players.registry.f()}.
}
\details{
\subsection{Latest snapshots}{
\itemize{
\item \code{players_m} and \code{players_f} are data frames containing the \strong{latest}
snapshot for Messieurs and Dames respectively.

Each snapshot data frame includes the following columns:
\itemize{
\item \code{"position"}: current AFTT ranking of the player
\item \code{"position_bis"}: ranking excluding inactive players
\item \code{"nom"} (if requested): player's name
\item \code{"classement"}: classement at start of season (or after mid‑season reevaluation)
\item \code{"club"}: club indice
\item \code{"match"}: number of matches played
\item \code{"points"}: points earned so far (basis for ranking)
\item \code{"licence"}: unique player ID
\item \code{"prov"}: province of the club
\item \code{"classement_lettre"}: classement letter (A, B, C, D, E, NC)
\item \code{"classement_chiffre"}: classement number (0, 2, 4, 6; 99 for A and NC)
\item \code{"position_bis_p"}: ranking of the player within their province
}

Each snapshot also includes attributes:
\itemize{
\item \code{year}: snapshot year
\item \code{month}: snapshot month
\item \code{retrieved}: POSIXct timestamp of retrieval
}
}
}

\subsection{Archives}{
\itemize{
\item \code{players_m_archive} and \code{players_f_archive} are \strong{named lists} of past snapshots.
Each element is named \code{"YYYY_MM"} and contains a data frame with the
same structure and attributes as the corresponding latest snapshot.
}
}
}
\examples{
\dontrun{
data(players_m)
head(players_m)
data(players_m_archive)
names(players_m_archive)
players_m_archive[["2026_04"]]
data(players_f)
head(players_f)
data(players_f_archive)
names(players_f_archive)
players_f_archive[["2026_04"]]
}

}
