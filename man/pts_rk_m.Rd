% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.pts_rk.R
\docType{data}
\name{pts_rk_m}
\alias{pts_rk_m}
\alias{pts_rk_f}
\title{Points–Ranking Reduced Tables}
\description{
These datasets store the reduced points–ranking tables generated by
\code{build.pts.rk()}. They contain only the information required for:
}
\details{
\itemize{
\item computing classement ranks from points via \code{points2rank()}, and
\item exporting JSON files used by the online tool.
}

For full player information (club, province, matches, etc.), use
\code{players_m} or \code{players_f}, which contain the complete registry snapshots.
\subsection{Contents}{
\itemize{
\item \code{pts_rk_m} is the points–ranking table for \strong{Messieurs}.
\item \code{pts_rk_f} is the points–ranking table for \strong{Dames}.
}

Each dataset is a two‑column data frame with:
\itemize{
\item \code{pts}: numeric points used for ranking
\item \code{rk}: integer ranking position (1 = highest ranked active player)
}

Only \strong{active} players are included (inactive players are excluded
upstream using \code{position_bis}).

These datasets are produced automatically by \code{build.pts.rk()} and
exported both as \code{.json} (for the web app) and \code{.rda} (for local use).
}
}
\examples{
# Messieurs
data(pts_rk_m)
head(pts_rk_m)

 \dontrun{# Dames
data(pts_rk_f)
head(pts_rk_f)
}

# Use in rank-from-points computation
points2rank(1500, pts_rk_m)

}
