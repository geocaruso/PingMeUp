% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.tree.R
\name{project.tree}
\alias{project.tree}
\title{Print a directory tree with optional exclusions}
\usage{
project.tree(path = ".", indent = "", exclude = c("dev"))
}
\arguments{
\item{path}{Character string. Root directory to print. Defaults to \code{"."}.}

\item{indent}{Internal parameter used for recursive indentation.
Users normally do not modify this.}

\item{exclude}{Character vector of folder or file names to exclude
(matched against \code{basename()}). Defaults to \code{"dev"}.}
}
\value{
Invisibly returns \code{NULL}. Called for its side effect of printing
the directory tree.
}
\description{
Recursively prints the folder structure of a project (typically the
package root), using an ASCII tree layout. Specific folders can be
excluded from the output.
}
\examples{
# Print the project tree, excluding the dev/ folder
project.tree(".", exclude = "dev")

# Exclude multiple folders
project.tree(".", exclude = c("dev", ".git", ".Rproj.user"))

}
